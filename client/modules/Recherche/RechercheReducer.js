// Import Actions
import {  } from './RechercheActions';

// Initial State
const initialState = {};

const RechercheReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default RechercheReducer;
